# Dependency directories (these can be re-installed with `npm install`)
node_modules/
frontend/node_modules/
backend/node_modules/

# Environment variables (these contain secrets!)
.env
frontend/.env
backend/.env

# Production build folder (this is generated during deployment)
frontend/build/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor and OS files
.vscode/
.DS_Store